package app.controllers;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;

public class PlayerActionWalkControllerTest {

	@Test public void fakeTest() {
		assertNotNull("put something real.", new PlayerActionWalkController(null, null, null));
 	}
}
